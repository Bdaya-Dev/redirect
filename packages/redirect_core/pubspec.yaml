name: redirect_core
description: >-
  Core types and interfaces for redirect-based flows.
  Platform-agnostic — works with pure Dart, Dart web, or Flutter.
version: 0.1.0
repository: https://github.com/Bdaya-Dev/redirect/tree/main/packages/redirect_core
topics:
  - redirect
  - deep-links
  - url-launcher
  - browser
  - callback

resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  meta: ^1.12.0

dev_dependencies:
  test: ^1.26.3
  very_good_analysis: ^10.1.0
