name: redirect_io
description: >-
  Pure Dart IO implementation of redirect-based flows.
  Ideal for command-line tools, servers, desktop apps, and non-Flutter Dart applications.
version: 0.1.0
repository: https://github.com/Bdaya-Dev/redirect/tree/main/packages/redirect_io
topics:
  - redirect
  - io
  - dart
  - deep-links
  - callback
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  meta: ^1.12.0
  redirect_core: ^0.1.0

dev_dependencies:
  test: ^1.26.3
  very_good_analysis: ^10.1.0
