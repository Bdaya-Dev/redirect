name: redirect_darwin
description: >-
  Shared iOS and macOS implementation of the redirect plugin.
  Uses ASWebAuthenticationSession for secure OAuth and redirect flows.
version: 0.1.0
repository: https://github.com/Bdaya-Dev/redirect/tree/main/packages/redirect_darwin
topics:
  - redirect
  - ios
  - macos
  - flutter-plugin
  - deep-links
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

flutter:
  plugin:
    implements: redirect
    platforms:
      ios:
        pluginClass: RedirectPlugin
        dartPluginClass: RedirectIosPlugin
        sharedDarwinSource: true
      macos:
        pluginClass: RedirectPlugin
        dartPluginClass: RedirectMacosPlugin
        sharedDarwinSource: true

dependencies:
  flutter:
    sdk: flutter
  redirect_platform_interface: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pigeon: ^26.1.7
  very_good_analysis: ^10.1.0
