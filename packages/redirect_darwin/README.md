# redirect_darwin

Shared iOS and macOS implementation of the redirect plugin. Uses ASWebAuthenticationSession for secure redirect-based flows.

## Installation

This package is endorsed by `redirect` and will be automatically included when you depend on `redirect` in a Flutter project targeting iOS or macOS.

## Usage

This package is not intended to be used directly. Use `redirect` instead.
