# redirect_desktop

Desktop (Linux/Windows) implementation of the redirect plugin. Uses a loopback HTTP server to handle redirect callbacks.

## Installation

This package is endorsed by `redirect` and will be automatically included when you depend on `redirect` in a Flutter project targeting Linux or Windows.

## Usage

This package is not intended to be used directly. Use `redirect` instead.
